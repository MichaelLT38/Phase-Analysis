
# Ignore everything by default
*

# But do not ignore directories, so Git can look inside them
# for the files we explicitly want to keep below.
!*/

# Keep Jupyter notebooks
!*.ipynb

# Keep PNG images
!*.png

# Keep this .gitignore itself
!.gitignore
